import type { CapacitorConfig } from '@capacitor/cli';

// TODO(#70): final rebrand — swap appId + appName before any store upload.
const config: CapacitorConfig = {
  appId: 'com.davbergen.lrpg',
  appName: 'LinguaQuest',
  webDir: 'dist',
};

export default config;
