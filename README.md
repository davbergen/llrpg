# LinguaQuest

A Japanese-learning RPG prototype built with Vite, React, and TypeScript. The app renders inside a fixed 390×720 phone frame and features an onboarding flow, home screen, vocabulary lessons, loot rewards, and a character profile — all styled with a pixel-art aesthetic using `Press Start 2P` and `Courier Prime` fonts.

```
npm install && npm run dev
```
